# Probo Configuration Template
# contains configuration options with some default values.
probod:
  hostname: "8vlq3we8mbakad4k.app.eng.kaamel.com"
  api:
    cors:
      allowed-origins: [ "http://localhost:8080", "http://localhost:5173" ]
  # use local minIO for now
  aws:
    access-key-id: "probod"
    secret-access-key: "thisisnotasecret"
    endpoint: "http://127.0.0.1:9000"
  mailer:
    sender-name: "Probo"
    sender-email: "no-reply@notification.localhost"
    smtp:
      addr: "localhost:1011"
      user: ""
      password: ""
      tls-required: false
  openai:
    api-key: ""
    temperature: 0
    model-name: "" 